# NeoMarket Moderation

Django service for NeoMarket moderation flows.

## Local run

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

## Tests

```bash
pytest
```
